import 'package:flutter/cupertino.dart';

class ListModel {
  String title;
  IconData icon;
  Color color;

  ListModel({
    required this.title,
    required this.icon,
    required this.color,
  });
}
